前端页面与 URL 对应关系（可直接访问）

1. 登录页面
   - 访问 URL：`/user/login`
   - 静态页面文件：`src/main/resources/static/pages/user-login.html`
   - 后端接口：`POST /api/user/login`

2. 注册页面
   - 访问 URL：`/user/login/register`
   - 静态页面文件：`src/main/resources/static/pages/user-register.html`
   - 后端接口：`POST /api/user/register`

3. 主页（待实现）
   - 访问 URL：`/main`
   - 静态页面文件：`src/main/resources/static/pages/main.html`
   - 规划后端接口：`GET /api/main/index`

4. 广场页面（待实现）
   - 规划访问 URL：`/main/ground/search/...`
   - 规划后端接口：`POST /api/main/uphoto/search`

5. 个人主页（展示）
   - 访问 URL：`/main/user/{uid}/display`
   - 静态页面文件：`src/main/resources/static/pages/user-display.html`
   - 规划后端接口：
     - `GET /api/user/activity/query`
     - `POST /api/user/uphoto/search`

6. 个人主页（编辑）
   - 访问 URL：`/main/user/edit/{uid}`
   - 静态页面文件：`src/main/resources/static/pages/user-edit.html`
   - 后端接口：
     - `PUT /api/user/update`
     - `DELETE /api/user/delete`

7. 管理员后台主页
   - 访问 URL：`/main/admin/edit/{uid}`
   - 静态页面文件：`src/main/resources/static/pages/admin.html`
   - 规划后端接口：
     - `GET /api/admin/activity/review`
     - `GET /api/admin/uphoto/review`
     - `PUT /api/user/update`
     - `PUT /api/admin/all_user/search`

后续在实现新的 Vue 页面时，请继续在本文件中追加对应关系，保持与 `接口文档.docx` 内 URL 规范一致。

1. 登录页面
   - 访问 URL：/user/login
   - 静态页面文件：src/main/resources/static/user-login.html
   - 后端接口：POST /api/user/login
2. 注册页面
   - 访问 URL：/user/login/register
   - 静态页面文件：src/main/resources/static/user-register.html
   - 后端接口：POST /api/user/register